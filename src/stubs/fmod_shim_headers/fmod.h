#pragma once
// fmod.h shim — Nival's FMSound.cpp `#include <fmod.h>` to get the FMOD 3.x
// C API declarations. The real fmod.h is proprietary. Our fmod_stub.h has
// the same symbol declarations.
#include "fmod_stub.h"
